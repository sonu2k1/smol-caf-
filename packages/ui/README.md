# @smol-cafe/ui

Shared UI component library for Smol Café.

## Purpose

Contains reusable, accessible UI primitives and design components shared across the web app and future apps/interfaces.

## Structure

- `src/`: Shared React components
- `src/index.ts`: Package entry point exporting all shared components
